import SettingsPage from '@/components/SettingsPage';

export const metadata = {
    title: 'Configurações - Dom TEA',
    description: 'Gerencie perfis, terapeutas, preferências e dados do sistema.',
};

export default function Settings() {
    return <SettingsPage />;
}
